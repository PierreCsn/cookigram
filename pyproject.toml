[project]
name = "cookigram-recipes"
version = "0.1.0"
description = "Carnet de recettes culinaires Gram, photographies et données nutritionnelles CookiGram"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = ["PyYAML>=6,<7"]
